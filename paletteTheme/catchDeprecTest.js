import { multiply } from './deprecTest.js';
// Deprecated: need to restart the experiment
function whatever(y, z) {
  console.log('');

  return multiply(y, z); // Deprecated: need to restart the experiment
}