/**
 * Дженерики (`Map<string, string>`): имя Map = cyan (ctor/entity),
 * параметры-примитивы = typeBuiltin, пользовательские = салатовый замок.
 */
export const genericTyping = {
  note: 'Map/Date/HttpException → syntax.ctor #5CCFE6; type args follow their own roles',
} as const;
