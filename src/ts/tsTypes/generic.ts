/**
 * Generics (`Map<string, string>`): name Map → ctor/class cyan;
 * type args: lib builtins → typeBuiltin cyan; custom names → salad lock.
 */
export const genericTyping = {
  containerHex: '#5CCFE6' as const,
  note: 'container via syntax.ctor / class.defaultLibrary; args keep own roles',
} as const;
