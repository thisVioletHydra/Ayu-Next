/**
 * Utility types in type position (`Readonly`, `Partial`, `Pick`).
 * Lavender `#D4BFFF` — never lime (lime is custom ThemeId/TokenDto only).
 */
export const utilityTyping = {
  role: 'syntax.typeBuiltin' as const,
  hex: '#D4BFFF' as const,
  examples: ['Readonly', 'Partial', 'Pick'] as const,
} as const;
