// import { toPathBase } from './toPath';
// import type { CodeId, ListRegexp } from '../index';

// type GetBaseName = { id: CodeId; listRegexp: ListRegexp };

// export function getBaseName({ id, listRegexp }: GetBaseName) {
//   const generatedPath = toPathBase(id);

//   return generatedPath.replace('src/', '').replaceAll(listRegexp.baseName, '_');
// }

export const getBaseName = 'error';
