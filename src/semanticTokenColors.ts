import semanticJson from '#data/semantic-token-colors.json' with { type: 'json' };

export const semanticHighlighting = semanticJson.semanticHighlighting;
export const semanticTokenColors = semanticJson.semanticTokenColors;
