{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ayu-next",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "preLaunchTask": "ayu-next: build",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/playground"
      ]
    }
  ]
}
